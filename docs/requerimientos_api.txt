**Título:** Requerimientos: Conexión API REST con Retrofit y Gson

**Descripción:**
Conectar la aplicación Android a la API REST del backend utilizando Retrofit para las peticiones HTTP y Gson para la serialización/deserialización de datos JSON.

---

**Fase 1: Lectura de Datos (GET)** `[COMPLETADA]`

**1.  Backend (Spring Boot):** `[COMPLETADO]`
    *   `[COMPLETADO]` Crear un `UsuarioDTO`.
    *   `[COMPLETADO]` Crear un `UsuarioController` con un endpoint `GET` para devolver el `UsuarioDTO`.

<<<<<<< HEAD
**2.  Frontend (Android - Kotlin):** `[COMPLETADO]`
    *   `[COMPLETADO]` Añadir las dependencias de Retrofit y Gson.
    *   `[COMPLETADO]` Crear (y fusionar) la interfaz `ApiService` con el endpoint `GET`.
    *   `[COMPLETADO]` Crear el `RetrofitClient`.
    *   `[COMPLETADO]` Actualizar `UsuarioViewModel` y `AuthScreen` para llamar y probar el endpoint `GET`.
=======
2.  **Frontend (Android - Kotlin):** `[PENDIENTE]`
    *   `[COMPLETADO]` Añadir las dependencias de Retrofit y Gson en `build.gradle.kts (app)`.
    *   `[COMPLETADO]` Crear una interfaz de servicio de Retrofit (`ApiService`) que defina los endpoints.
    *   `[COMPLETADO]` Crear una instancia de Retrofit (Retrofit Client) configurada con la URL base del backend y el convertidor de Gson.
    *   `[PENDIENTE]` Actualizar la lógica de la aplicación (ViewModels, Repositories) para usar el cliente de Retrofit.
>>>>>>> 4c9666aede8a5a6c7ca4e03ba9d19183b2614567

---

**Fase 2: Escritura de Datos (POST - Registro)** `[PENDIENTE]`

**1.  Backend (Spring Boot):** `[PENDIENTE]`
    *   `[PENDIENTE]` Crear un endpoint `POST` en `UsuarioController` para registrar nuevos usuarios.
    *   `[PENDIENTE]` La lógica debe recibir un `UsuarioDTO`, convertirlo a una entidad `Usuario`, hashear la contraseña y guardarlo en la base de datos.

**2.  Frontend (Android - Kotlin):** `[PENDIENTE]`
    *   `[PENDIENTE]` Añadir un método `POST` en la `ApiService` para el registro de usuarios.
    *   `[PENDIENTE]` Modificar la función `register` en `UsuarioViewModel` para que llame al nuevo endpoint de la API, reemplazando la lógica de la base de datos local (`dbHelper`).

---

**Avances Realizados (Fase 1 - Backend):**

**1. UsuarioKotlinDTO.java:**
```java
package com.example.SaveUp.DTO;

import java.util.Date;
import lombok.Data;
import lombok.AllArgsConstructor;
import lombok.NoArgsConstructor;

@Data
@AllArgsConstructor
@NoArgsConstructor
public class UsuarioKotlinDTO {
    private String rut;
    private String nombre;
    private String apellido;
    private String email;
    private String contrasena;
    private Date fechaRegistro;
}
```

**2. UsuarioKotlinController.java (GET):**
```java
package com.example.SaveUp.controller;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

import com.example.SaveUp.DTO.UsuarioKotlinDTO;
import com.example.SaveUp.model.Usuario;
import com.example.SaveUp.service.UsuarioService;

@RestController
@RequestMapping("/api/usuariosK")
public class UsuarioKotlinController {

    @Autowired
    private UsuarioService usuarioService;

    @GetMapping("/{rut}")
    public ResponseEntity<UsuarioKotlinDTO> getUsuarioByRut(@PathVariable String rut) {
        Usuario usuarioEntity = usuarioService.findByRut(rut);

        if (usuarioEntity == null) {
            return ResponseEntity.notFound().build();
        }

        UsuarioKotlinDTO usuarioDTO = convertToDto(usuarioEntity);
        return ResponseEntity.ok(usuarioDTO);
    }

    private UsuarioKotlinDTO convertToDto(Usuario entity) {
        return new UsuarioKotlinDTO(
            entity.getRut(),
            entity.getNombre(),
            entity.getApellido(),
            entity.getEmail(),
            null,  // Se omite la contraseña en la respuesta
            entity.getFechaRegistro()
        );
    }
}
```