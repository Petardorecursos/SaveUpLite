package com.example.saveuplite.ui.theme

import androidx.compose.ui.graphics.Color

// Paleta de Colores - Soft UI / Neumorfismo
// Basada en la especificación del usuario.

// Fondo Principal
val SoftWhite = Color(0xFFF8F8FA) // Blanco muy suave, casi puro con un toque cálido.
val LightGray = Color(0xFFE8E8ED) // Gris muy claro alternativo.

// Color Primario (Acento)
val LavenderBlue = Color(0xFF7E8CE7) // Azul lavanda desaturado.
val DesaturatedPurple = Color(0xFF8F99F5) // Púrpura desaturado alternativo.

// Acentos Secundarios (Tarjetas de Contenido)
val PalePink = Color(0xFFF8E7EA)    // Rosa / Salmón Pálido (para "Breakfast", "IBM")
val PaleSalmon = Color(0xFFFAEAEF)   // Alternativa al rosa pálido.
val PaleAqua = Color(0xFFE0F5F2)      // Verde Azulado / Aqua Pálido (para "Sport Data", "Sleep Quality")
val PaleTeal = Color(0xFFE2F5F4)      // Alternativa al aqua pálido.
val MediumBlue = Color(0xFFBCBFFA)    // Azul Medio (para "Hearth Health")
val LightBlueBg = Color(0xFFEAEBFC)   // Alternativa al azul medio.
val DangerRed = Color(0xFFA63636)
val DarkTeal = Color(0xFF00796B) // Verde azulado oscuro para texto de ingresos

// Gradientes
val GradientStart = Color(0xFF8F99F5) // Azul para gradiente.
val GradientEnd = Color(0xFFC89EFD)   // Lila para gradiente.

// Texto
val DarkGrayText = Color(0xFF333333) // Gris muy oscuro, casi negro.
val NearBlackText = Color(0xFF404040) // Alternativa al gris oscuro.
val MediumGrayText = Color(0xFF888888) // Gris medio para subtexto.
val LightGrayText = Color(0xFFA0A0A0)  // Alternativa al gris medio.